# 🔴 راهنمای فوری رفع مشکل

## مشکل اصلی
- بکند در حال اجرا نیست یا crash می‌کند
- Frontend داده نمی‌گیرد
- Admin Panel ارور 429 می‌دهد

## ✅ راه حل فوری

### مرحله 1: بکند را راه‌اندازی کنید

```bash
cd backend

# اگر dependencies نصب نشده
npm install

# Reset و setup دیتابیس
npx prisma migrate reset --force
npx prisma migrate dev
npx prisma db seed

# اجرای بکند
npm run dev
```

**بکند باید روی پورت 5000 اجرا شود و این پیام را ببینید:**
```
🚀 Server running on http://localhost:5000
📊 API available at http://localhost:5000/api
```

### مرحله 2: تست کنید

در مرورگر باز کنید:
- `http://localhost:5000/api/health` → باید `{"status":"ok"}` ببینید
- `http://localhost:5000/api/products` → باید لیست محصولات را ببینید

### مرحله 3: Frontend را اجرا کنید

در یک terminal جدید:
```bash
cd next-app
npm run dev
```

باز کنید: `http://localhost:3000`

### مرحله 4: Admin Panel را اجرا کنید

در یک terminal جدید:
```bash
cd admin-panel
npm run dev
```

باز کنید: `http://localhost:3001`

## 🔧 تغییرات اعمال شده

1. ✅ **Rate Limiting**: در development کاملاً غیرفعال شد
2. ✅ **Error Handling**: Parsing JSON با try-catch محافظت شد
3. ✅ **Variants**: بررسی null/undefined اضافه شد

## ⚠️ اگر هنوز مشکل دارید

### مشکل: بکند crash می‌کند

Console بکند را بررسی کنید. خطاها را ببینید.

ممکن است نیاز باشد:
```bash
cd backend
rm -rf node_modules
npm install
npx prisma generate
npm run dev
```

### مشکل: ارور 429 هنوز هست

1. بکند را کاملاً stop کنید (Ctrl+C)
2. دوباره start کنید
3. مطمئن شوید `NODE_ENV` تنظیم نشده

### مشکل: داده‌ها نیست

```bash
cd backend
npx prisma db seed
```

## 📞 بررسی سریع

```bash
# بررسی بکند
curl http://localhost:5000/api/health

# بررسی محصولات
curl http://localhost:5000/api/products | head -20

# بررسی تعداد محصولات در دیتابیس
cd backend
sqlite3 prisma/dev.db "SELECT COUNT(*) FROM Product;"
```

## 🎯 ترتیب اجرا

**همیشه این ترتیب را رعایت کنید:**

1. **اول:** Backend (پورت 5000)
2. **دوم:** Frontend (پورت 3000)  
3. **سوم:** Admin Panel (پورت 3001)

## 💡 نکته مهم

اگر بکند در حال اجرا نیست، **هیچ چیز کار نمی‌کند**. همیشه ابتدا بکند را اجرا کنید.





